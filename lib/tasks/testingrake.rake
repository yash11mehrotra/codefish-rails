namespace :testingrake do
  desc "TODO"
  task test1: :environment do
  	
  	# Rake::Task['morning:make_coffee'].invoke
  	# rake db:migrate
  	# exec( "git status" )
  	# exec("rake db:drop")
  	# exec("rake db:migrate:status && rake db:migrate && rake assets:precompile")
  	
  	# exec("rake assets:precompile")
  	# puts "test1 task has been execued"
  	# puts "test1 task has been execued"
# exec( terminal )
	# sh 'ls' 
	# sh 'rake db:migrate'
	# sh 'rake db:migrate:status'
  end

end
